# Cloudflare Pages (Git / wrangler pages deploy). Do not use Workers static-assets config here.
# SPA routing: omit 404.html in dist; Pages serves index.html for unknown routes.
# Do NOT add public/_redirects rules to /index.html (Cloudflare error 100324).
name = "gymweek-front"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"
