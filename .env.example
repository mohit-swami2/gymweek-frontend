# Frontend environment (local dev)
# Leave empty in dev — Vite proxy forwards /api to the backend.
# Cloudflare Pages: set in Dashboard → Settings → Environment variables (Production + Preview)
VITE_API_URL=

# Example production value:
# VITE_API_URL=https://api.gymweek.app
